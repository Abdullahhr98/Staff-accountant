// Placeholder auth provider (expand for real auth)
import 'package:flutter/material.dart';

class AuthProvider extends ChangeNotifier {
  bool get isLoggedIn => false;

  // Implement FirebaseAuth integration later
}
