name: staff_accountant
description: Employee Payroll Accountant — Flutter scaffold
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  firebase_storage: ^11.0.0
  google_sign_in: ^6.0.0
  intl: ^0.18.0
  flutter_localizations:
    sdk: flutter
  url_launcher: ^6.1.7
  pdf: ^3.10.1
  printing: ^5.10.1
  path_provider: ^2.0.11
  shared_preferences: ^2.1.1
  flutter_secure_storage: ^8.0.0
  encrypt: ^5.0.1
  file_picker: ^5.2.3
  syncfusion_flutter_charts: ^20.2.45
  timezone: ^0.8.0
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/i18n/
